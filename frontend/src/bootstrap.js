const bootstrap = async (path) => {
  try {
    let module

    switch (path) {
      case value:
        
        break
    
      default:
        break
    }

  } catch (error) {
    console.log(error, 'Error loading module.')
  }
}

export default bootstrap